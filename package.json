{
  "name": "node-opcua-github-io-root",
  "version": "2.0.0",
  "private": true,
  "description": "Root helper package to orchestrate Astro subproject scripts",
  "scripts": {
    "dev": "npm --prefix v2 run dev",
    "start": "npm --prefix v2 run dev",
    "build": "npm --prefix v2 run build",
    "preview": "npm --prefix v2 run preview"
  }
}
